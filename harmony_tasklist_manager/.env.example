# HarmonyOS Task List Manager - Environment Variables

# Configuration file path (optional, defaults to ./config.yaml)
# HARMONY_TASKLIST_CONFIG=/path/to/config.yaml

# Data file paths (override config.yaml values)
# HARMONY_DATA_FILE=data/tasklist.txt
# HARMONY_TITLE_FILE=data/title.txt

# Log level (DEBUG, INFO, WARNING, ERROR)
# HARMONY_LOG_LEVEL=INFO
